<h1>New Review for <%= link_to @restaurant.name, restaurant_path(@restaurant) %></h1>

<%= simple_form_for([ @restaurant, @review ]) do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit "Add review", class: "btn btn-primary" %>
<% end %>
